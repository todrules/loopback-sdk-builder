<%- buildModModImports(models) %>

@NgModule({
imports:      [ CommonModule ],
declarations: [
<%
Object.keys(models).forEach(function(modelName, i, arr) {
    modelName = modelName[ 0 ].toUpperCase() + modelName.slice(1);
%>
<%- modelName %> <%= i < arr.length - 1 ? ',' : '' %> <%
}); %>
],
exports: [
<%
Object.keys(models).forEach(function(modelName, i, arr) {
    modelName = modelName[ 0 ].toUpperCase() + modelName.slice(1);
%>
<%- modelName %> <%= i < arr.length - 1 ? ',' : '' %> <%
}); %>
],
providers:    [ ]
})

export class ModelModule {

constructor() {

}
}

